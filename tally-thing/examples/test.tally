;;let func = step another(dont, fail) third [more
;;  stuff in
;;between  ] here okay
;;ballots by(constituency) [fptp] {
;;  largest-party# = results-by-party [count] highest#
	;;constituencies = _
;;}

let _ = BUILTIN(_identity)
let by(key) = BUILTIN(_by, key)

.ballots by(constituency) _