fn fib(n: int) -> int {
    if n <= 0 {
        return n;
    } else {
        return fib(n-1) + fib(n-2);
    }
}

fn main() {
    for i: int in 0..10 {
        let f: int = fib(i);
        println("fib(" + int2str(i) + ") = " + int2str(f));
    }
}
